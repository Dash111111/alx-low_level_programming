#include<stdio.h>
/**
 *main - Entry point
 *void Return(int);
 *Return; always 0 (success)
 *return 0;
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	Return(0);
}
